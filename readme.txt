User: admin
password: umami

#Replace tracking code in customHead.html file within the custom-files folder with new tracking code from localhost

#Sprint 4 
run docker compose up --build
change your site domain name on umami to mysite.local instead of localhost replace tracking code in custom-files folder if needed.
run docker compose down then docker compose up once again 